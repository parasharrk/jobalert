<%= form_for(@job_stream) do |f| %>
  <% if @job_stream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_stream.errors.count, "error") %> prohibited this job_stream from being saved:</h2>

      <ul>
      <% @job_stream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
